{
  "name": "toldosporrino-mantenimiento",
  "version": "1.0.0",
  "description": "App de gestión de contratos de mantenimiento – Toldos Porriño",
  "scripts": {
    "build": "echo 'App estática – no requiere build'",
    "sync": "npx cap sync",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
